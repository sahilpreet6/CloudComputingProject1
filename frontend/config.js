/*
 * ---------------------------------------------------------------------------
 *  SUPABASE CONFIG  (shared by login.html and the dashboard)
 * ---------------------------------------------------------------------------
 *  Paste your project's values below. Both are safe to expose in client code
 *  (the anon key is a public key protected by Row Level Security).
 *
 *  Find them in the Supabase dashboard:
 *    Project Settings -> API -> "Project URL" and "anon public" key.
 * ---------------------------------------------------------------------------
 */
window.SUPABASE_CONFIG = {
  url: "",      // e.g. "https://abcdefghijklmno.supabase.co"
  anonKey: "",  // e.g. "eyJhbGciOiJIUzI1NiIsInR5cCI6..."
};
